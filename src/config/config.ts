// ? 全局不动配置项 只做导出不做修改

// * 首页地址（默认）
export const HOME_URL = '/index'

// * 登录页地址（默认）
export const LOGIN_URL = '/login'

// * 默认页编剧
export const DEFAULT_PADDING = '35px'

// * 路由白名单地址（必须是本地存在的路由 staticRouter.ts）
export const ROUTER_WHITE_LIST: string[] = ['/404']
